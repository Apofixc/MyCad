{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default permissions for MyCad window",
  "windows": ["*"],
  "permissions": [
    "core:default",
    "dialog:default"
  ]
}
